## Assembler for Custom RISC ISA

Simple assembler written in Python. It assembles instructions to both Logisim and VHDL rom format for my custom ISA processor implementations.

Usage: python assembler2.py <assembly_textfile> <starting_address>
